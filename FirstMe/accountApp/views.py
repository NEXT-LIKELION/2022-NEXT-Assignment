from django.shortcuts import render, redirect
from django.contrib import auth

# Create your views here.
# 로그아웃 기능만 여기에 넣을 것 (auth.logout)

# def logout(request):
#     auth.logout(request)
#     return redirect('home')